<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8"/>
    <title>BZFlag Player Portal</title>
    <style>
        header {
            margin-bottom: 2em;
        }

        div.error, div.flash {
            -webkit-border-radius: 8px;
            -moz-border-radius: 8px;
            border-radius: 8px;
            display: inline-block;
            padding: 5px;
            margin: 5px;
        }

        div.error {
            color: red;
            background-color: lightgrey;
            border: 1px solid darkred;
        }

        div.flash {
            border: 1px solid black;
        }
    </style>
</head>
<body>
<header>
    <nav>
        <ul>
            <li><a href="{{ path_for('home') }}">Home</a></li>
            <li><a href="{{ path_for('organizations') }}">Organizations</a></li>
        </ul>
    </nav>
    <h1>BZFlag Player Portal</h1>
{% if user.username %}
    <div>Logged in as {{ user.username }}. <a href="{{ path_for('logout') }}">Logout?</a></div>
{% else %}
    <div>You are not logged in. <a href="{{ path_for('login') }}">Log in?</a></div>
{% endif %}
</header>
{% if flash_message %}
<div class="flash">
    {{ flash_message }}
</div>
{% endif %}

{% if error %}<div class="error">{{ error }}</div>{% endif %}

{% block content %}{% endblock %}

</body>
</html>
